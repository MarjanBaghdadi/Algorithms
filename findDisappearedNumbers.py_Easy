class Solution:
    def findDisappearedNumbers(self, nums: List[int]) -> List[int]:


        inputList=set(nums)
        solution=list()
        for i in range(len(nums)):
            if i+1 not in inputList:
                solution.append(i+1)
        return solution
